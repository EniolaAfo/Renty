rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access to the locations collection
    match /locations/{document=**} {
      allow read, write: if true;
    }
  }
}
